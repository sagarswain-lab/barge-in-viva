Record two short WAV files and place them here before running tests/test_interrupt.py:

- student_turn_1.wav   — a normal, real viva answer, ~8-10 seconds, mono, 16kHz+ recommended
- interrupt_1.wav      — a short interrupting phrase like "wait, can you clarify that?", ~2-3 seconds

Use real recordings, not synthetic beeps — the hackathon brief specifically
wants representative test conditions, not toy stand-ins.
